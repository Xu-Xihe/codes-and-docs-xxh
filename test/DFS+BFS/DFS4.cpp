#include<cstdio>
#define maxe 100
using namespace std;
int len,in[maxe];
void bfs(int )
int main(){
    scanf("%d",&len);
    for (int i = 0; i < len; i++)
    {
        scanf("%d",&in[i]);
    }
    dfs();
    return 0;
}