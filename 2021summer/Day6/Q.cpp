#include <cstdio>
const int maxe = 1e5 + 9;
int n, m, st[maxe][20],log[maxe];
int main()
{
    scanf("%d%d", &n, &m);
    for (int i = 0; i < n; i++)
    {
        scanf("%d", &st[i][0]);
    }
    for (int i = 0; i < n; i++)
    {
        log[i]=
    }
    
}